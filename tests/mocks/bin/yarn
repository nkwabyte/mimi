#!/bin/sh
# Mock: yarn — Yarn Classic shape. `yarn cache dir` prints a sandbox path.
case "$1" in
  --version) printf '1.22.99\n'; exit 0 ;;
  cache)
    if [ "$2" = "dir" ]; then printf '%s\n' "${TMPDIR:-/tmp}/yarn-cache-mock"; exit 0; fi
    exit 0 ;;
  *) exit 0 ;;
esac
